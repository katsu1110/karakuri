{
  "name": "karakuri",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build-articles.js && node scripts/generate-feed.js && node scripts/generate-sitemap.js && vite build",
    "feed:fetch": "node scripts/fetch-papers.js",
    "feed:draft": "node scripts/draft-papers.js",
    "feed:autoapprove": "node scripts/auto-approve.js",
    "feed:publish": "node scripts/publish-papers.js",
    "guest:resolve": "node scripts/resolve-author.js",
    "guest:works": "node scripts/fetch-guest-works.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.0",
    "gray-matter": "^4.0.3",
    "marked": "^16.0.0"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
